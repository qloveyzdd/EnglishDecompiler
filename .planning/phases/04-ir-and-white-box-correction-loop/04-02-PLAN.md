---
phase: 04-ir-and-white-box-correction-loop
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/App.tsx
  - src/components/demo/token-pane.tsx
  - src/components/demo/role-chip.tsx
  - src/components/demo/json-pane.tsx
  - src/components/demo/ir-pane.tsx
  - src/components/demo/token-inspector.tsx
  - src/components/demo/role-picker.tsx
  - src/lib/demo.ts
  - tests/white-box-correction-loop.test.tsx
  - tests/setup.ts
  - scripts/validation/validate-phase-04.ps1
autonomous: true
requirements: [IR-01, IR-02, EDIT-01, EDIT-02, EDIT-03, EDIT-04]
user_setup: []
must_haves:
  truths:
    - "The Phase 3 blank-first and explicit-parse behavior stays intact even after adding correction and persistence."
    - "Every manual role change updates visible token state, JSON, and IR from the same corrected result immediately."
    - "Restored local corrections only apply when the same sentence is parsed again, never as a global token rule."
  artifacts:
    - path: "src/components/demo/ir-pane.tsx"
      provides: "IR output surface stacked with JSON"
      contains: "Code-like IR"
    - path: "src/components/demo/token-inspector.tsx"
      provides: "Contextual explanation surface with rule id"
      contains: "Why this role"
    - path: "tests/white-box-correction-loop.test.tsx"
      provides: "UI contract tests for editing and persistence"
      contains: "Set role: action"
    - path: "scripts/validation/validate-phase-04.ps1"
      provides: "One-command phase validation entrypoint"
      contains: "pnpm build"
  key_links:
    - from: "src/App.tsx"
      to: "src/lib/corrections.ts"
      via: "corrected parse state"
      pattern: "applyRoleOverrides("
    - from: "src/App.tsx"
      to: "src/lib/ir.ts"
      via: "stacked IR output"
      pattern: "renderIr("
    - from: "tests/white-box-correction-loop.test.tsx"
      to: "src/App.tsx"
      via: "token edit and persistence assertions"
      pattern: "render(<App />)"
---

<objective>
Integrate the correction loop into the visible workbench and lock it behind interaction tests.

Purpose: Turn the existing demo into an editable reasoning surface where token corrections, explanation, JSON, IR, and local persistence all work together without breaking Phase 3 behavior.
Output: Updated workbench UI, interactive correction flow, sentence-scoped persistence in the browser, and a one-command validation path for the whole phase.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ir-and-white-box-correction-loop/04-CONTEXT.md
@.planning/phases/04-ir-and-white-box-correction-loop/04-RESEARCH.md
@.planning/phases/04-ir-and-white-box-correction-loop/04-UI-SPEC.md
@.planning/phases/04-ir-and-white-box-correction-loop/04-VALIDATION.md
@src/App.tsx
@src/components/demo/token-pane.tsx
@src/components/demo/json-pane.tsx
@src/lib/corrections.ts
@src/lib/correction-storage.ts
@src/lib/ir.ts
@tests/local-demo-v0.test.tsx
@AGENTS.md
</context>

<threat_model>
## Trust Boundaries
- boundary: token chip interaction -> corrected parse state
  data: selected token index, manual role choice, explanation copy
- boundary: corrected parse state -> visible JSON and IR
  data: corrected `ParseResult`, formatted JSON string, IR string
- boundary: browser storage -> restored corrections after refresh
  data: saved sentence overrides, persisted input key

## Threat Register
- threat_id: T-04-04
  category: Integrity
  component: interactive correction UI
  disposition: mitigate
  mitigation_plan: Drive token chips, selected-token details, JSON, and IR from the same corrected state owned by `App.tsx`, and expose exactly six explicit role choices in the UI.
- threat_id: T-04-05
  category: Repudiation
  component: explainability surface
  disposition: mitigate
  mitigation_plan: Show token text, current role, `rule id`, and a short reason in one dedicated detail component so the interface never hides why a role is assigned or corrected.
- threat_id: T-04-06
  category: Availability
  component: validation and persistence path
  disposition: mitigate
  mitigation_plan: Add interaction tests for selection, correction, regeneration, and persistence, plus a PowerShell validation script that runs install, check, test, and build from repo root.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Integrate token selection, role editing, and contextual explanation into the token pane</name>
  <files>src/App.tsx, src/components/demo/token-pane.tsx, src/components/demo/role-chip.tsx, src/components/demo/token-inspector.tsx, src/components/demo/role-picker.tsx, src/lib/demo.ts</files>
  <read_first>.planning/phases/04-ir-and-white-box-correction-loop/04-UI-SPEC.md, src/App.tsx, src/components/demo/token-pane.tsx, src/lib/corrections.ts, src/lib/demo.ts</read_first>
  <action>Update `src/App.tsx` so it owns `baseResult`, `correctedResult`, `selectedTokenIndex`, and the current sentence overrides. After `parseSentence(draftInput.trim())` succeeds, load saved overrides through `loadSentenceCorrections(trimmedInput)`, build `correctedResult = applyRoleOverrides(baseResult, overrides)`, and derive `formattedJson` plus `renderIr(correctedResult)` from that corrected result only. Create `src/components/demo/token-inspector.tsx` that shows the selected token text, current role, `Rule: {span.rule}`, and the explanation string from `describeSpanRole()`. Create `src/components/demo/role-picker.tsx` that renders exactly six action buttons named `Set role: action`, `Set role: object`, `Set role: relation`, `Set role: condition`, `Set role: purpose`, and `Set role: unknown`. Update `src/components/demo/role-chip.tsx` so each token chip is a button with `aria-pressed` driven by selection state. Update `src/components/demo/token-pane.tsx` so clicking a token selects it, shows `token-inspector`, and exposes `role-picker` for the selected token only. If a selected token has a manual override, the inspector must include the exact label `Saved locally`.</action>
  <verify>if (!(Test-Path src/components/demo/token-inspector.tsx) -or !(Test-Path src/components/demo/role-picker.tsx)) { exit 1 }; $appPatterns = @('applyRoleOverrides(','loadSentenceCorrections(','selectedTokenIndex','renderIr('); foreach ($pattern in $appPatterns) { Select-String -Path src/App.tsx -Pattern [regex]::Escape($pattern) | Out-Null }; $chipPatterns = @('aria-pressed','button'); foreach ($pattern in $chipPatterns) { Select-String -Path src/components/demo/role-chip.tsx -Pattern [regex]::Escape($pattern) | Out-Null }; $pickerPatterns = @('Set role: action','Set role: object','Set role: relation','Set role: condition','Set role: purpose','Set role: unknown'); foreach ($pattern in $pickerPatterns) { Select-String -Path src/components/demo/role-picker.tsx -Pattern [regex]::Escape($pattern) | Out-Null }; Select-String -Path src/components/demo/token-inspector.tsx -Pattern 'Why this role' | Out-Null</verify>
  <acceptance_criteria>
    - `src/App.tsx` calls `applyRoleOverrides(` and `renderIr(`
    - `src/App.tsx` loads saved overrides through `loadSentenceCorrections(`
    - `src/components/demo/role-chip.tsx` renders interactive button chips with `aria-pressed`
    - `src/components/demo/token-inspector.tsx` contains `Why this role`
    - `src/components/demo/role-picker.tsx` contains all six `Set role:` button labels
  </acceptance_criteria>
  <done>The middle pane becomes the visible correction loop instead of a read-only token list.</done>
</task>

<task type="auto">
  <name>Task 2: Stack JSON and IR in the output pane and persist sentence-scoped corrections</name>
  <files>src/App.tsx, src/components/demo/json-pane.tsx, src/components/demo/ir-pane.tsx, src/lib/correction-storage.ts</files>
  <read_first>.planning/phases/04-ir-and-white-box-correction-loop/04-UI-SPEC.md, src/App.tsx, src/components/demo/json-pane.tsx, src/lib/correction-storage.ts, src/lib/ir.ts</read_first>
  <action>Create `src/components/demo/ir-pane.tsx` with a heading `Code-like IR` and a code surface that renders the exact IR string produced by `renderIr(correctedResult)`. Keep `src/components/demo/json-pane.tsx` focused on JSON only, but update `src/App.tsx` so the right pane shell becomes `Structured Outputs` with stacked `Structured JSON` then `Code-like IR` sections. On every manual role change, immediately call `saveSentenceCorrections(trimmedInput, overrides)` and rebuild both `formattedJson` and the IR string from the corrected result. Preserve the Phase 3 stale-output rule: editing the textarea after a successful parse must still clear JSON, IR, selection, and copy state until the next parse succeeds. If the same trimmed sentence is parsed again after reload, the saved overrides must rehydrate automatically; if the sentence changes, no saved override may leak across.</action>
  <verify>if (!(Test-Path src/components/demo/ir-pane.tsx)) { exit 1 }; Select-String -Path src/components/demo/ir-pane.tsx -Pattern 'Code-like IR' | Out-Null; $appPatterns = @('Structured Outputs','saveSentenceCorrections(','setSelectedTokenIndex(null)','setCopyState(\"idle\")'); foreach ($pattern in $appPatterns) { Select-String -Path src/App.tsx -Pattern [regex]::Escape($pattern) | Out-Null }</verify>
  <acceptance_criteria>
    - `src/components/demo/ir-pane.tsx` exists and contains `Code-like IR`
    - `src/App.tsx` contains `Structured Outputs`
    - `src/App.tsx` calls `saveSentenceCorrections(`
    - `src/App.tsx` clears `selectedTokenIndex` when the input changes after a parse
    - JSON and IR are rendered from the same corrected result path
  </acceptance_criteria>
  <done>The right pane becomes a synchronized JSON + IR output surface with sentence-scoped persistence.</done>
</task>

<task type="auto">
  <name>Task 3: Add interaction tests and a one-command validation path for the correction loop</name>
  <files>tests/white-box-correction-loop.test.tsx, tests/setup.ts, scripts/validation/validate-phase-04.ps1</files>
  <read_first>tests/local-demo-v0.test.tsx, tests/setup.ts, scripts/validation/validate-phase-03.ps1, src/App.tsx</read_first>
  <action>Create `tests/white-box-correction-loop.test.tsx` covering these exact behaviors: parsing `Initialize the model before training.` renders both JSON and `Code-like IR`; clicking a token selects it and reveals `Why this role`; clicking one of the `Set role:` buttons updates the visible role chip plus the JSON output; manual correction updates the visible IR string too; rerendering the app after saving an override and parsing the same sentence restores the corrected role from `localStorage`; editing the textarea after a successful parse clears JSON, IR, and the token inspector; `Copy JSON` still copies the exact visible JSON string after corrections. Update `tests/setup.ts` only as needed to reset clipboard and `localStorage` state between tests. Create `scripts/validation/validate-phase-04.ps1` with the same command order as Phase 3: `pnpm install`, `pnpm check`, `pnpm test`, `pnpm build`, plus the `corepack pnpm` fallback and one final success line only after all commands pass.</action>
  <verify>if (!(Test-Path tests/white-box-correction-loop.test.tsx) -or !(Test-Path scripts/validation/validate-phase-04.ps1)) { exit 1 }; $testPatterns = @('Code-like IR','Why this role','Set role: action','navigator.clipboard.writeText','localStorage','Initialize the model before training.'); foreach ($pattern in $testPatterns) { Select-String -Path tests/white-box-correction-loop.test.tsx -Pattern [regex]::Escape($pattern) | Out-Null }; $scriptPatterns = @('pnpm install','pnpm check','pnpm test','pnpm build','corepack pnpm'); foreach ($pattern in $scriptPatterns) { Select-String -Path scripts/validation/validate-phase-04.ps1 -Pattern [regex]::Escape($pattern) | Out-Null }</verify>
  <acceptance_criteria>
    - `tests/white-box-correction-loop.test.tsx` asserts the `Code-like IR` surface
    - The test file asserts `Why this role`
    - The test file asserts at least one `Set role:` action
    - The test file asserts `navigator.clipboard.writeText`
    - `scripts/validation/validate-phase-04.ps1` contains `pnpm install`, `pnpm check`, `pnpm test`, `pnpm build`, and `corepack pnpm`
  </acceptance_criteria>
  <done>The full correction loop is test-covered and re-runnable through one obvious command path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Token chips are interactive and expose one selected correction surface at a time
- [ ] JSON and IR are stacked and both derive from corrected parse state
- [ ] Sentence-scoped persistence restores only same-sentence overrides
- [ ] Interaction tests cover selection, correction, regeneration, persistence, and corrected JSON copy behavior
- [ ] `scripts/validation/validate-phase-04.ps1` runs install, check, test, and build in one path
</verification>

<success_criteria>

- All tasks completed
- The demo keeps its blank-first honesty while becoming directly editable
- Manual corrections immediately regenerate JSON and IR
- Explanation and provenance remain visible for the selected token
- Saved corrections survive refresh for the same sentence only
</success_criteria>

<output>
After completion, create `.planning/phases/04-ir-and-white-box-correction-loop/04-02-SUMMARY.md`
</output>
