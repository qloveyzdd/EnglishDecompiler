---
phase: 03-local-demo-v0
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/App.tsx
  - src/components/demo/example-chips.tsx
  - src/components/demo/json-pane.tsx
  - src/components/demo/token-pane.tsx
  - tests/local-demo-v0.test.tsx
  - tests/setup.ts
  - scripts/validation/validate-phase-03.ps1
  - vite.config.ts
autonomous: true
requirements: [DEMO-01, DEMO-02]
user_setup: []
must_haves:
  truths:
    - "Example helpers remain secondary: the page still opens blank and examples never auto-run parsing."
    - "Copy JSON must copy the exact visible `ParseResult` payload, not a synthetic or reformatted variant."
    - "Editing the input after a successful parse clears stale output and requires a new explicit parse action."
  artifacts:
    - path: "tests/local-demo-v0.test.tsx"
      provides: "UI interaction contract tests"
      contains: "Parse Sentence"
    - path: "scripts/validation/validate-phase-03.ps1"
      provides: "One-command phase validation entrypoint"
      contains: "pnpm build"
    - path: "src/App.tsx"
      provides: "Example, reset, and copy interaction flow"
      contains: "navigator.clipboard.writeText"
  key_links:
    - from: "tests/local-demo-v0.test.tsx"
      to: "src/App.tsx"
      via: "blank-state and explicit-parse assertions"
      pattern: "render(<App />)"
    - from: "scripts/validation/validate-phase-03.ps1"
      to: "package.json"
      via: "script-driven validation"
      pattern: "pnpm test"
---

<objective>
Finish the local demo interaction loop and lock it behind lightweight automated verification.

Purpose: Complete the parts of Phase 3 that make the page demonstrable instead of just rendered: example helpers, copy feedback, stale-output reset, and one-command local validation.
Output: A complete local demo interaction flow, component tests for `DEMO-01` and `DEMO-02`, and a Windows-friendly validation script.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-demo-v0/03-CONTEXT.md
@.planning/phases/03-local-demo-v0/03-RESEARCH.md
@.planning/phases/03-local-demo-v0/03-UI-SPEC.md
@.planning/phases/03-local-demo-v0/03-VALIDATION.md
@fixtures/parser-core-v0.ts
@src/App.tsx
@AGENTS.md
</context>

<threat_model>
## Trust Boundaries
- boundary: example helpers -> user input state
  data: demo example sentences, textarea contents, parse trigger
- boundary: visible JSON pane -> clipboard
  data: formatted `ParseResult` string
- boundary: UI behavior -> automated checks
  data: test assertions, validation script exit codes, build output

## Threat Register
- threat_id: T-03-04
  category: Integrity
  component: copy JSON action
  disposition: mitigate
  mitigation_plan: Drive the copy action from the same formatted JSON string rendered in the pane, disable copy before a successful parse, and test the clipboard payload directly.
- threat_id: T-03-05
  category: Repudiation
  component: interaction contract
  disposition: mitigate
  mitigation_plan: Add Vitest + Testing Library coverage for blank initial state, explicit parse trigger, example fill-only behavior, and stale-output reset so future changes cannot silently alter the demo contract.
- threat_id: T-03-06
  category: Availability
  component: local validation path
  disposition: mitigate
  mitigation_plan: Provide a single PowerShell validation entrypoint that runs install, typecheck, tests, and production build without relying on ad hoc shell history.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add example helpers and finish the explicit-parse interaction states</name>
  <files>src/App.tsx, src/components/demo/example-chips.tsx, src/components/demo/token-pane.tsx, src/components/demo/json-pane.tsx</files>
  <read_first>.planning/phases/03-local-demo-v0/03-UI-SPEC.md, fixtures/parser-core-v0.ts, src/App.tsx</read_first>
  <action>Update the demo so it exposes exactly three example helper chips built from `fixtures/parser-core-v0.ts`: `Initialize the model before training.`, `Run the server with the config.`, and `If the server uses the config, load the file.`. Clicking one of those helpers must write that sentence into the textarea and must not trigger parsing automatically. Add explicit input-reset behavior: when the textarea changes after a successful parse, clear the stored parse result and any copy status immediately. Keep the token and JSON panes in empty state until the next parse succeeds. Move any pane-specific rendering into small helper components only if that reduces duplication; do not introduce a router, a global store, or extra screens.</action>
  <verify>if (!(Test-Path src/components/demo/example-chips.tsx)) { exit 1 }; $examplePatterns = @('Initialize the model before training.','Run the server with the config.','If the server uses the config, load the file.'); foreach ($pattern in $examplePatterns) { Select-String -Path src/components/demo/example-chips.tsx -Pattern [regex]::Escape($pattern) | Out-Null }; $appPatterns = @('setResult(null)','setCopyState(\"idle\")'); foreach ($pattern in $appPatterns) { Select-String -Path src/App.tsx -Pattern [regex]::Escape($pattern) | Out-Null }</verify>
  <acceptance_criteria>
    - `src/components/demo/example-chips.tsx` contains the exact three approved example sentences
    - Clicking an example only fills input; no code path calls `parseSentence` from the example click handler
    - `src/App.tsx` clears the last parse result on post-parse input edits
    - `src/App.tsx` clears copy state on post-parse input edits
  </acceptance_criteria>
  <done>The page becomes screenshot-ready without breaking the blank-first, explicit-parse contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement copy feedback and add UI interaction tests</name>
  <files>src/App.tsx, tests/local-demo-v0.test.tsx, tests/setup.ts, vite.config.ts</files>
  <read_first>.planning/phases/03-local-demo-v0/03-UI-SPEC.md, src/App.tsx, package.json</read_first>
  <action>Implement the `Copy JSON` interaction so it calls `navigator.clipboard.writeText(formattedJson)` where `formattedJson` is the exact string rendered in the JSON pane. The copy button must stay disabled before a successful parse, switch to the success label for `1500ms` after a successful copy, and surface the approved inline failure copy if clipboard access fails. Configure Vitest in `vite.config.ts` with a `jsdom` environment and a `tests/setup.ts` file. Create `tests/local-demo-v0.test.tsx` covering these exact behaviors: initial blank textarea with disabled parse/copy controls; clicking `Parse Sentence` after typing a known sentence renders token output and JSON output; clicking an example chip fills the textarea without rendering output; editing the textarea after a successful parse clears output panes; clicking `Copy JSON` writes the exact visible JSON string to the clipboard.</action>
  <verify>if (!(Test-Path tests/local-demo-v0.test.tsx) -or !(Test-Path tests/setup.ts)) { exit 1 }; $testPatterns = @('Parse Sentence','Copy JSON','navigator.clipboard.writeText','Initialize the model before training.','Run the server with the config.','If the server uses the config, load the file.'); foreach ($pattern in $testPatterns) { Select-String -Path tests/local-demo-v0.test.tsx -Pattern [regex]::Escape($pattern) | Out-Null }; $vitePatterns = @('test:','environment: \"jsdom\"'); foreach ($pattern in $vitePatterns) { Select-String -Path vite.config.ts -Pattern [regex]::Escape($pattern) | Out-Null }</verify>
  <acceptance_criteria>
    - `tests/local-demo-v0.test.tsx` asserts the blank initial state with disabled parse and copy controls
    - `tests/local-demo-v0.test.tsx` asserts example chips fill the textarea without auto-parsing
    - `tests/local-demo-v0.test.tsx` asserts input edits clear prior output
    - `tests/local-demo-v0.test.tsx` asserts `navigator.clipboard.writeText` receives the visible JSON string
    - `vite.config.ts` contains Vitest config with `environment: "jsdom"`
  </acceptance_criteria>
  <done>`DEMO-01` and `DEMO-02` are protected by automated interaction checks instead of screenshots alone.</done>
</task>

<task type="auto">
  <name>Task 3: Add a one-command local validation path for the demo phase</name>
  <files>scripts/validation/validate-phase-03.ps1</files>
  <read_first>package.json, vite.config.ts, tests/local-demo-v0.test.tsx, scripts/validation/validate-phase-02.ps1</read_first>
  <action>Create `scripts/validation/validate-phase-03.ps1` as a simple PowerShell script that runs exactly these commands from repo root in order: `pnpm install`, `pnpm check`, `pnpm test`, and `pnpm build`. Keep the same fallback behavior used in Phase 2 so the script substitutes `corepack pnpm` when `pnpm` is not on PATH. Make the script print which command is running, stop on the first failure, and end with one success line only after all commands pass.</action>
  <verify>if (!(Test-Path scripts/validation/validate-phase-03.ps1)) { exit 1 }; $patterns = @('pnpm install','pnpm check','pnpm test','pnpm build','corepack pnpm'); foreach ($pattern in $patterns) { Select-String -Path scripts/validation/validate-phase-03.ps1 -Pattern [regex]::Escape($pattern) | Out-Null }</verify>
  <acceptance_criteria>
    - `scripts/validation/validate-phase-03.ps1` contains `pnpm install`
    - `scripts/validation/validate-phase-03.ps1` contains `pnpm check`
    - `scripts/validation/validate-phase-03.ps1` contains `pnpm test`
    - `scripts/validation/validate-phase-03.ps1` contains `pnpm build`
    - `scripts/validation/validate-phase-03.ps1` contains the `corepack pnpm` fallback
  </acceptance_criteria>
  <done>The phase can be revalidated locally through one obvious command path on the current Windows setup.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Example helpers use the approved three-sentence set and never auto-run parse
- [ ] Copy JSON uses the same formatted payload rendered in the JSON pane
- [ ] Vitest covers blank-state, parse, example-fill, reset, and copy behaviors
- [ ] `scripts/validation/validate-phase-03.ps1` runs install, check, test, and build in one path
</verification>

<success_criteria>

- All tasks completed
- The page remains blank-first but is still self-demonstrating through secondary example helpers
- `Copy JSON` is obvious, deterministic, and test-covered
- Local contributors can verify the demo with one command
- Phase 4 can extend this demo surface instead of re-litigating basic interaction rules
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-demo-v0/03-02-SUMMARY.md`
</output>
