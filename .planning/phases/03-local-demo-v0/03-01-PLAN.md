---
phase: 03-local-demo-v0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - index.html
  - components.json
  - src/main.tsx
  - src/app.css
  - src/lib/utils.ts
  - src/App.tsx
autonomous: true
requirements: [DEMO-01]
user_setup: []
must_haves:
  truths:
    - "The demo stays browser-only and consumes the existing parser contract instead of recreating parser logic in the UI."
    - "The first render is blank-first: no prefilled sentence, no fake parse result, and no automatic parsing on keystroke."
    - "The page shell reflects the approved three-pane workbench contract and remains responsive by stacking in the same order on narrow screens."
  artifacts:
    - path: "package.json"
      provides: "Vite + React demo scripts and dependency manifest"
      contains: "\"dev\": \"vite\""
    - path: "vite.config.ts"
      provides: "React + Tailwind Vite configuration with alias support"
      contains: "plugins: [react(), tailwindcss()]"
    - path: "components.json"
      provides: "shadcn baseline config"
      contains: "\"style\""
    - path: "src/main.tsx"
      provides: "React entrypoint"
      contains: "createRoot"
    - path: "src/App.tsx"
      provides: "Parser-backed three-pane demo shell"
      contains: "parseSentence"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React root render"
      pattern: "render("
    - from: "src/App.tsx"
      to: "src/index.ts"
      via: "parser contract import"
      pattern: "from \"./index.js\""
    - from: "vite.config.ts"
      to: "tsconfig.app.json"
      via: "shared @ alias"
      pattern: "\"@/*\""
---

<objective>
Bootstrap the local React demo runtime and wire the existing parser into a stable three-pane shell.

Purpose: Turn the repo from parser-only into a runnable local demo without breaking the Phase 2 library contract.
Output: A Vite + React + TypeScript workspace, shadcn baseline configuration, and a blank-first workbench page that calls `parseSentence` only when the user clicks the parse action.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-demo-v0/03-CONTEXT.md
@.planning/phases/03-local-demo-v0/03-RESEARCH.md
@.planning/phases/03-local-demo-v0/03-UI-SPEC.md
@src/index.ts
@src/parser/types.ts
@fixtures/parser-core-v0.ts
@AGENTS.md
</context>

<threat_model>
## Trust Boundaries
- boundary: parser library -> browser demo shell
  data: `ParseResult`, `ParseRole`, parser summary data
- boundary: local package scripts -> contributors and maintainers
  data: dev server, build output, typecheck output
- boundary: initial page state -> user understanding of product behavior
  data: empty state copy, parse controls, placeholder content

## Threat Register
- threat_id: T-03-01
  category: Tampering
  component: parser contract usage
  disposition: mitigate
  mitigation_plan: Import `parseSentence` and parser types directly from `src/index.ts`, keep role metadata centralized, and do not create a second parse schema inside the UI.
- threat_id: T-03-02
  category: Availability
  component: local demo runtime
  disposition: mitigate
  mitigation_plan: Add explicit Vite, app TypeScript, and alias configuration so `pnpm dev`, `pnpm build`, and `pnpm check` remain deterministic on the current Windows-first setup.
- threat_id: T-03-03
  category: Integrity
  component: blank-first interaction contract
  disposition: mitigate
  mitigation_plan: Keep the textarea empty on first render, disable the parse action until non-empty input exists, and show instructional placeholders instead of fake parse output.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Vite + React + Tailwind + shadcn baseline</name>
  <files>package.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts, index.html, components.json, src/main.tsx, src/app.css, src/lib/utils.ts</files>
  <read_first>.planning/phases/03-local-demo-v0/03-CONTEXT.md, .planning/phases/03-local-demo-v0/03-RESEARCH.md, .planning/phases/03-local-demo-v0/03-UI-SPEC.md, package.json, tsconfig.json, AGENTS.md</read_first>
  <action>Update `package.json` to add exactly these scripts: `"dev": "vite"`, `"build": "tsc -p tsconfig.json && vite build"`, `"preview": "vite preview"`, `"check": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.app.json --noEmit && tsc -p tsconfig.node.json --noEmit"`, and `"test": "vitest run"`. Add runtime dependencies `react`, `react-dom`, `lucide-react`, `class-variance-authority`, `clsx`, `tailwind-merge`, and `tw-animate-css`. Add dev dependencies `vite`, `@vitejs/plugin-react`, `tailwindcss`, `@tailwindcss/vite`, `shadcn`, `vitest`, `@testing-library/react`, `@testing-library/user-event`, `jsdom`, `@types/react`, `@types/react-dom`, and `@types/node`. Keep `tsconfig.json` as the parser build config, but add `"baseUrl": "."` and `"paths": { "@/*": ["./src/*"] }` to `compilerOptions`. Create `tsconfig.app.json` with `jsx: "react-jsx"`, `noEmit: true`, and `include` patterns for `src/**/*.ts` and `src/**/*.tsx`. Create `tsconfig.node.json` for `vite.config.ts` with `noEmit: true` and NodeNext settings. Create `vite.config.ts` with `react()` and `tailwindcss()` plugins plus the `@` alias to `./src`. Create `index.html` with a single `<div id="root"></div>`. Create `src/main.tsx` that imports `StrictMode`, `createRoot`, `./app.css`, and renders `<App />`. Create `src/app.css` as the Tailwind entry file. Create `src/lib/utils.ts` exporting the standard `cn` helper. After the Vite files exist, run `pnpm dlx shadcn@latest init -p base-nova -y` so `components.json` is generated against this repo instead of a throwaway sample app.</action>
  <verify>if (!(Test-Path package.json) -or !(Test-Path tsconfig.app.json) -or !(Test-Path tsconfig.node.json) -or !(Test-Path vite.config.ts) -or !(Test-Path index.html) -or !(Test-Path components.json) -or !(Test-Path src/main.tsx) -or !(Test-Path src/app.css) -or !(Test-Path src/lib/utils.ts)) { exit 1 }; $packagePatterns = @('\"dev\": \"vite\"','\"build\": \"tsc -p tsconfig.json && vite build\"','\"preview\": \"vite preview\"','\"check\": \"tsc -p tsconfig.json --noEmit && tsc -p tsconfig.app.json --noEmit && tsc -p tsconfig.node.json --noEmit\"','\"test\": \"vitest run\"','\"react\"','\"react-dom\"','\"@vitejs/plugin-react\"','\"tailwindcss\"','\"@tailwindcss/vite\"','\"shadcn\"'); foreach ($pattern in $packagePatterns) { Select-String -Path package.json -Pattern [regex]::Escape($pattern) | Out-Null }; $vitePatterns = @('react()','tailwindcss()','path.resolve(__dirname, \"./src\")'); foreach ($pattern in $vitePatterns) { Select-String -Path vite.config.ts -Pattern [regex]::Escape($pattern) | Out-Null }; $tsPatterns = @('\"baseUrl\": \".\"','\"@/*\"'); foreach ($pattern in $tsPatterns) { Select-String -Path tsconfig.json -Pattern [regex]::Escape($pattern) | Out-Null }; Select-String -Path src/main.tsx -Pattern 'createRoot' | Out-Null; Select-String -Path src/app.css -Pattern '@import \"tailwindcss\"' | Out-Null</verify>
  <acceptance_criteria>
    - `package.json` contains `dev`, `build`, `preview`, `check`, and `test` scripts with the exact values above
    - `tsconfig.json` keeps the parser build settings and adds the `@/*` alias
    - `tsconfig.app.json` and `tsconfig.node.json` both exist
    - `vite.config.ts` contains `react()`, `tailwindcss()`, and the `@` alias
    - `index.html` contains `id="root"`
    - `components.json` exists after `shadcn init`
    - `src/main.tsx` renders `<App />` through `createRoot`
    - `src/app.css` contains `@import "tailwindcss";`
  </acceptance_criteria>
  <done>The repo can run a React/Vite local demo without sacrificing the existing parser build path.</done>
</task>

<task type="auto">
  <name>Task 2: Build the blank-first three-pane parser workbench</name>
  <files>src/App.tsx, src/components/demo/pane-shell.tsx, src/components/demo/role-legend.tsx, src/components/demo/role-chip.tsx, src/lib/demo.ts</files>
  <read_first>.planning/phases/03-local-demo-v0/03-UI-SPEC.md, .planning/phases/03-local-demo-v0/03-CONTEXT.md, src/index.ts, src/parser/types.ts, fixtures/parser-core-v0.ts</read_first>
  <action>Create `src/App.tsx` and any small demo helpers needed to render the approved workbench using shadcn `Button`, `Card`, `Textarea`, `Badge`, `ScrollArea`, and `Separator`. Import `parseSentence` and `ParseResult` from `./index.js`; do not duplicate parser roles locally. Render exactly three pane titles: `Source Sentence`, `Token Roles`, and `Structured JSON`. Use a blank textarea as the initial state. Add one primary parse button whose visible label is the phase-approved parse CTA and whose `disabled` state is driven by `!draftInput.trim()`. When clicked, it must call `parseSentence(draftInput.trim())` and store that result for the token and JSON panes. On desktop, render the three panes in one row using the approved width ratio `1.05fr 1.1fr 1fr`; on narrow screens, stack them in the same order. Before any parse exists, show instructional empty-state content in the token and JSON panes instead of fake sample output. Create a role metadata helper in `src/lib/demo.ts` so every `ParseRole` maps to one consistent label and style class. Render token chips in original span order with token text plus role label, and render the JSON pane with `JSON.stringify(result, null, 2)` inside a monospace code surface.</action>
  <verify>if (!(Test-Path src/App.tsx) -or !(Test-Path src/lib/demo.ts)) { exit 1 }; $appPatterns = @('Source Sentence','Token Roles','Structured JSON','parseSentence(draftInput.trim())','disabled={!draftInput.trim()}','JSON.stringify(result, null, 2)','xl:grid-cols-[1.05fr_1.1fr_1fr]'); foreach ($pattern in $appPatterns) { Select-String -Path src/App.tsx -Pattern [regex]::Escape($pattern) | Out-Null }; $demoPatterns = @('action','object','relation','condition','purpose','unknown'); foreach ($pattern in $demoPatterns) { Select-String -Path src/lib/demo.ts -Pattern [regex]::Escape($pattern) | Out-Null }</verify>
  <acceptance_criteria>
    - `src/App.tsx` imports `parseSentence` from `./index.js`
    - `src/App.tsx` contains the pane titles `Source Sentence`, `Token Roles`, and `Structured JSON`
    - `src/App.tsx` contains `parseSentence(draftInput.trim())`
    - `src/App.tsx` disables the parse button when the trimmed input is empty
    - `src/App.tsx` uses the desktop ratio `1.05fr 1.1fr 1fr`
    - `src/App.tsx` renders JSON through `JSON.stringify(result, null, 2)`
    - `src/lib/demo.ts` declares metadata for all six parser roles
  </acceptance_criteria>
  <done>The local demo visibly exposes the existing parser contract through a stable workbench shell.</done>
</task>

<task type="auto">
  <name>Task 3: Add parser summary and role-visual helpers without changing parse semantics</name>
  <files>src/components/demo/summary-strip.tsx, src/components/demo/role-chip.tsx, src/lib/demo.ts</files>
  <read_first>src/parser/types.ts, src/App.tsx, .planning/phases/03-local-demo-v0/03-UI-SPEC.md</read_first>
  <action>Create a compact summary strip component that reads directly from `ParseResult.summary` and shows action/object/relation/condition/purpose values only when they exist. Keep role styling low-saturation and semantic: `action`, `object`, `relation`, `condition`, `purpose`, and `unknown` each need their own helper class. The role chip component must render the original token text first and the role label second, and it must preserve `unknown` spans instead of filtering them out. Keep all demo helpers read-only with respect to parser data: no token regrouping, no synthetic spans, and no post-parse role reassignment.</action>
  <verify>if (!(Test-Path src/components/demo/summary-strip.tsx) -or !(Test-Path src/components/demo/role-chip.tsx)) { exit 1 }; Select-String -Path src/components/demo/summary-strip.tsx -Pattern 'summary' | Out-Null; $roleChipPatterns = @('span.text','span.role'); foreach ($pattern in $roleChipPatterns) { Select-String -Path src/components/demo/role-chip.tsx -Pattern [regex]::Escape($pattern) | Out-Null }; Select-String -Path src/lib/demo.ts -Pattern 'unknown' | Out-Null</verify>
  <acceptance_criteria>
    - `src/components/demo/summary-strip.tsx` reads from `result.summary`
    - `src/components/demo/role-chip.tsx` renders `span.text` and `span.role`
    - `src/lib/demo.ts` still includes `unknown` as a first-class role style
    - No new file in this task writes back into parser source files
  </acceptance_criteria>
  <done>The phase gets consistent visual semantics for parser output without mutating parser behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `package.json` defines a runnable Vite demo path and a multi-tsconfig `check` script
- [ ] `vite.config.ts`, `tsconfig.app.json`, and `components.json` exist with alias-aware Vite + shadcn wiring
- [ ] `src/App.tsx` renders the blank-first three-pane shell and calls `parseSentence` only on explicit parse
- [ ] Role metadata and summary helpers render the parser contract without inventing new parser semantics
</verification>

<success_criteria>

- All tasks completed
- `pnpm dev` starts a real local demo instead of a parser-only package
- The UI shell is blank-first, explicit-parse, and visibly aligned with the approved three-pane contract
- The demo imports the existing parser contract instead of forking it
- Phase 4 can extend the same app shell rather than replacing it
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-demo-v0/03-01-SUMMARY.md`
</output>
