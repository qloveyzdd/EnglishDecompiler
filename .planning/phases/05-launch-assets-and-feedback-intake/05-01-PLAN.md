---
phase: 05-launch-assets-and-feedback-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/hero.gif
  - assets/social-preview.png
  - assets/launch/demo-overview.png
  - assets/launch/correction-loop.png
  - assets/launch/json-ir-view.png
  - assets/launch/asset-manifest.json
  - examples/launch-examples.json
autonomous: true
requirements: [REPO-03]
user_setup: []
must_haves:
  truths:
    - "Every launch asset must show behavior already validated in Phases 3 and 4 instead of hinting at broader parser scope."
    - "The canonical public examples corpus lives in `examples/` and stays focused on real technical-document style single sentences."
    - "The README hero entry path remains `./assets/hero.gif`; Phase 5 upgrades the asset without moving the public path."
  artifacts:
    - path: "examples/launch-examples.json"
      provides: "Canonical public sentence corpus for launch and future parser-example intake"
      contains: "\"sentence\""
    - path: "assets/launch/asset-manifest.json"
      provides: "Machine-readable mapping between launch assets and the demo states they show"
      contains: "\"hero\""
    - path: "assets/social-preview.png"
      provides: "Repository social preview image"
      contains: ""
    - path: "assets/hero.gif"
      provides: "Primary README hero media"
      contains: ""
  key_links:
    - from: "assets/launch/asset-manifest.json"
      to: "assets/hero.gif"
      via: "hero asset mapping"
      pattern: "\"hero\""
    - from: "assets/launch/asset-manifest.json"
      to: "examples/launch-examples.json"
      via: "asset source sentences"
      pattern: "\"exampleIds\""
    - from: "assets/launch/asset-manifest.json"
      to: "assets/social-preview.png"
      via: "social preview mapping"
      pattern: "\"socialPreview\""
---

<objective>
Create the canonical launch asset pack and public examples corpus for the current MVP.

Purpose: Turn the validated demo into a repo-browsable launch surface that quickly communicates the white-box parser workflow without claiming broader capability than the product actually has.
Output: A polished hero GIF, three stable launch images, a social preview image, a machine-readable asset manifest, and a canonical examples file under `examples/`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-launch-assets-and-feedback-intake/05-CONTEXT.md
@.planning/phases/05-launch-assets-and-feedback-intake/05-RESEARCH.md
@.planning/phases/05-launch-assets-and-feedback-intake/05-VALIDATION.md
@.planning/phases/04-ir-and-white-box-correction-loop/04-02-SUMMARY.md
@.planning/phases/04-ir-and-white-box-correction-loop/04-UAT.md
@README.md
@assets/hero.gif
@src/App.tsx
@src/components/demo/token-pane.tsx
@src/components/demo/token-inspector.tsx
@src/components/demo/role-picker.tsx
@src/components/demo/json-pane.tsx
@src/components/demo/ir-pane.tsx
@AGENTS.md
</context>

<threat_model>
## Trust Boundaries
- boundary: validated demo state -> launch media
  data: input sentence, token roles, correction UI, JSON, IR
- boundary: canonical examples -> public expectations
  data: example sentences, categories, notes, launch-relevant IDs
- boundary: README hero path -> committed asset pack
  data: `assets/hero.gif`, screenshot files, social preview file

## Threat Register
- threat_id: T-05-01
  category: Integrity
  component: launch media accuracy
  disposition: mitigate
  mitigation_plan: Build every asset from Phase 4-validated single-sentence demo states and avoid any asset that implies multi-sentence parsing, hidden AI assistance, or unsupported parser scope.
- threat_id: T-05-02
  category: Availability
  component: public asset paths
  disposition: mitigate
  mitigation_plan: Keep the public hero path as `assets/hero.gif`, place the rest of the fixed asset pack under `assets/launch/`, and record all filenames in `assets/launch/asset-manifest.json`.
- threat_id: T-05-03
  category: Integrity
  component: canonical examples corpus
  disposition: mitigate
  mitigation_plan: Store a sentence-first examples file under `examples/launch-examples.json` with technical-doc oriented entries only, and tie launch assets back to those example IDs in the asset manifest.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical launch examples corpus</name>
  <files>examples/launch-examples.json</files>
  <read_first>.planning/phases/05-launch-assets-and-feedback-intake/05-CONTEXT.md, .planning/phases/05-launch-assets-and-feedback-intake/05-RESEARCH.md, README.md, CONTRIBUTING.md, src/parser/types.ts, src/App.tsx</read_first>
  <action>Create `examples/launch-examples.json` as the canonical public corpus for Phase 5. Use a top-level object with exactly these keys: `"version"`, `"focus"`, and `"examples"`. Set `"version"` to `"0.1"` and `"focus"` to `"single-sentence technical English"`. Populate `"examples"` with at least 12 entries across these exact categories: `setup-guide`, `api-doc`, `github-readme`, and `ai-tooling-doc`. Every entry must include exactly these fields: `"id"`, `"category"`, `"sentence"`, `"whyItMatters"`, and `"launchFit"`. Keep all sentences as single technical-document style sentences that are honest fits for the current parser and demo. Make at least three entries use the known good launch/demo sentences from earlier phases, including `Initialize the model before training.` and `If the server uses the config, load the file.`.</action>
  <verify>if (!(Test-Path examples/launch-examples.json)) { exit 1 }; $patterns = @('\"version\": \"0.1\"','\"focus\": \"single-sentence technical English\"','\"category\": \"setup-guide\"','\"category\": \"api-doc\"','\"category\": \"github-readme\"','\"category\": \"ai-tooling-doc\"','Initialize the model before training.','If the server uses the config, load the file.'); foreach ($pattern in $patterns) { Select-String -Path examples/launch-examples.json -Pattern [regex]::Escape($pattern) | Out-Null }</verify>
  <acceptance_criteria>
    - `examples/launch-examples.json` exists
    - The file contains `"version": "0.1"` and `"focus": "single-sentence technical English"`
    - The file contains all four required categories
    - The file contains `Initialize the model before training.`
    - The file contains `If the server uses the config, load the file.`
    - Every entry uses the exact field names `id`, `category`, `sentence`, `whyItMatters`, and `launchFit`
  </acceptance_criteria>
  <done>The repo has one obvious, extensible examples corpus instead of scattering launch examples across prose.</done>
</task>

<task type="auto">
  <name>Task 2: Create the fixed launch image set and social preview</name>
  <files>assets/social-preview.png, assets/launch/demo-overview.png, assets/launch/correction-loop.png, assets/launch/json-ir-view.png</files>
  <read_first>.planning/phases/04-ir-and-white-box-correction-loop/04-UAT.md, src/App.tsx, src/components/demo/token-pane.tsx, src/components/demo/token-inspector.tsx, src/components/demo/role-picker.tsx, src/components/demo/json-pane.tsx, src/components/demo/ir-pane.tsx, examples/launch-examples.json</read_first>
  <action>Create these exact image files: `assets/social-preview.png`, `assets/launch/demo-overview.png`, `assets/launch/correction-loop.png`, and `assets/launch/json-ir-view.png`. `demo-overview.png` must show the three-pane workbench after parsing `Initialize the model before training.`. `correction-loop.png` must show a selected token, the role picker, `Why this role`, and the `Saved locally` state after one manual role change. `json-ir-view.png` must visibly show stacked `Structured JSON` and `Code-like IR`. `social-preview.png` must contain the project name `English Decompiler`, the line `Parse technical English like code`, and a sentence -> structure -> IR visual direction that matches the repo positioning. Keep the assets bright, tool-like, and honest to the current product surface.</action>
  <verify>if (!(Test-Path assets/social-preview.png) -or !(Test-Path assets/launch/demo-overview.png) -or !(Test-Path assets/launch/correction-loop.png) -or !(Test-Path assets/launch/json-ir-view.png)) { exit 1 }</verify>
  <acceptance_criteria>
    - `assets/social-preview.png` exists
    - `assets/launch/demo-overview.png` exists
    - `assets/launch/correction-loop.png` exists
    - `assets/launch/json-ir-view.png` exists
    - The screenshot set covers parse overview, correction loop, and JSON/IR output separately
  </acceptance_criteria>
  <done>The repo has a fixed launch image set that can be linked from README and GitHub previews.</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade the hero GIF and record the asset manifest</name>
  <files>assets/hero.gif, assets/launch/asset-manifest.json</files>
  <read_first>README.md, assets/hero.gif, examples/launch-examples.json, assets/social-preview.png, assets/launch/demo-overview.png, assets/launch/correction-loop.png, assets/launch/json-ir-view.png</read_first>
  <action>Replace the placeholder `assets/hero.gif` with a polished launch GIF that shows the approved white-box loop in this exact order: paste a single technical sentence, parse it, select a token, change a role, and show JSON/IR updating. Create `assets/launch/asset-manifest.json` with exactly these top-level keys: `"hero"`, `"screenshots"`, `"socialPreview"`, and `"exampleIds"`. Set `"hero"` to `"assets/hero.gif"`. Set `"socialPreview"` to `"assets/social-preview.png"`. Populate `"screenshots"` with the three exact asset paths from Task 2. Populate `"exampleIds"` with the example IDs used by the hero and screenshot assets so later repo copy can point to stable launch examples.</action>
  <verify>if (!(Test-Path assets/hero.gif) -or !(Test-Path assets/launch/asset-manifest.json)) { exit 1 }; $patterns = @('\"hero\": \"assets/hero.gif\"','\"socialPreview\": \"assets/social-preview.png\"','assets/launch/demo-overview.png','assets/launch/correction-loop.png','assets/launch/json-ir-view.png','\"exampleIds\"'); foreach ($pattern in $patterns) { Select-String -Path assets/launch/asset-manifest.json -Pattern [regex]::Escape($pattern) | Out-Null }</verify>
  <acceptance_criteria>
    - `assets/hero.gif` exists and remains at the same public path
    - `assets/launch/asset-manifest.json` exists
    - The manifest contains the exact keys `hero`, `screenshots`, `socialPreview`, and `exampleIds`
    - The manifest points to all three screenshot files and `assets/social-preview.png`
  </acceptance_criteria>
  <done>The launch pack has one canonical manifest and preserves the stable README hero path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `examples/launch-examples.json` exists with the required schema and technical-doc sentence coverage
- [ ] `assets/social-preview.png` and the three screenshot files all exist
- [ ] `assets/hero.gif` remains the public README asset path
- [ ] `assets/launch/asset-manifest.json` maps assets back to stable filenames and example IDs
</verification>

<success_criteria>

- All tasks completed
- The repo has a durable, honest launch asset pack rather than placeholder media
- The public examples corpus is obvious, inspectable, and reusable
- The README hero path stays stable while the launch media quality improves
- Phase 5 public copy can reference fixed asset and example filenames without guessing
</success_criteria>

<output>
After completion, create `.planning/phases/05-launch-assets-and-feedback-intake/05-01-SUMMARY.md`
</output>
