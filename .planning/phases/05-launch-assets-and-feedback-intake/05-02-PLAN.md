---
phase: 05-launch-assets-and-feedback-intake
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - README.md
  - README.zh-CN.md
  - CONTRIBUTING.md
  - .github/ISSUE_TEMPLATE/parser_example.md
  - .github/ISSUE_TEMPLATE/config.yml
  - scripts/validation/validate-phase-05.ps1
autonomous: true
requirements: [REPO-03, COMM-01]
user_setup: []
must_haves:
  truths:
    - "Public repo entry points must link to the exact launch assets and examples created in Plan 01, not placeholder or aspirational files."
    - "GitHub issues remain the only public feedback intake path in this phase; `good first issue` is surfaced through label-aware links rather than a new submission form."
    - "Phase 5 validation checks both launch-package integrity and existing app health without introducing heavyweight automation."
  artifacts:
    - path: "README.md"
      provides: "English-first launch entry with examples and feedback routes"
      contains: "Examples"
    - path: ".github/ISSUE_TEMPLATE/config.yml"
      provides: "Issue landing flow with beginner-friendly route"
      contains: "good first issue"
    - path: "scripts/validation/validate-phase-05.ps1"
      provides: "One-command validation for assets, links, and repo health"
      contains: "Phase 05 validation passed."
  key_links:
    - from: "README.md"
      to: "examples/launch-examples.json"
      via: "public examples entry"
      pattern: "Examples"
    - from: "README.md"
      to: "assets/hero.gif"
      via: "hero media"
      pattern: "![Hero demo]"
    - from: ".github/ISSUE_TEMPLATE/config.yml"
      to: "https://github.com"
      via: "good first issue route"
      pattern: "good first issue"
---

<objective>
Wire the launch asset pack into the public repo surfaces and make the feedback loop obvious.

Purpose: Turn the repo into a release-ready GitHub package where visitors can quickly find examples, assets, and issue paths for bugs, parser examples, and beginner-friendly contributions.
Output: Updated README surfaces, tightened issue routing, and a phase-specific validation script that protects the whole launch package.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-launch-assets-and-feedback-intake/05-CONTEXT.md
@.planning/phases/05-launch-assets-and-feedback-intake/05-RESEARCH.md
@.planning/phases/05-launch-assets-and-feedback-intake/05-VALIDATION.md
@.planning/phases/01-repository-positioning-and-launch-skeleton/01-CONTEXT.md
@README.md
@README.zh-CN.md
@CONTRIBUTING.md
@.github/ISSUE_TEMPLATE/parser_example.md
@.github/ISSUE_TEMPLATE/config.yml
@assets/launch/asset-manifest.json
@examples/launch-examples.json
@AGENTS.md
</context>

<threat_model>
## Trust Boundaries
- boundary: repo entry pages -> new visitors
  data: launch promise, examples link, feedback link, asset references
- boundary: issue landing flow -> contributors
  data: parser-example route, bug route, good-first-issue route
- boundary: validation script -> release readiness
  data: file existence checks, link checks, pnpm health checks

## Threat Register
- threat_id: T-05-04
  category: Integrity
  component: launch-facing repo copy
  disposition: mitigate
  mitigation_plan: Update README surfaces to point only to real files from Plan 01 and keep the copy inside the previously locked white-box parser positioning.
- threat_id: T-05-05
  category: Availability
  component: public feedback routes
  disposition: mitigate
  mitigation_plan: Keep feedback inside GitHub issue flows, expose the parser-example template clearly, and add a label-filtered `good first issue` route in the issue landing config.
- threat_id: T-05-06
  category: Repudiation
  component: launch-package verification
  disposition: mitigate
  mitigation_plan: Add `scripts/validation/validate-phase-05.ps1` so asset presence, repo links, and the existing app checks run through one repeatable command.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update the public README surfaces to expose launch assets and examples</name>
  <files>README.md, README.zh-CN.md</files>
  <read_first>.planning/phases/05-launch-assets-and-feedback-intake/05-CONTEXT.md, .planning/phases/01-repository-positioning-and-launch-skeleton/01-CONTEXT.md, README.md, README.zh-CN.md, assets/launch/asset-manifest.json, examples/launch-examples.json</read_first>
  <action>Update `README.md` so the first screen links to the canonical examples corpus and the launch assets created in Plan 01. Keep the existing project positioning and the `![Hero demo](./assets/hero.gif)` path. Add an `Examples` entry in the top link row that points to `./examples/launch-examples.json`. Add one short launch-assets section that points to the screenshot set and social preview using the exact filenames from `assets/launch/asset-manifest.json`. Add one short feedback section that points visitors to the GitHub issue paths for `bug` and `parser examples wanted`. Update `README.zh-CN.md` so it mirrors the same links while still clearly positioning the English README as the primary external entry.</action>
  <verify>if (!(Test-Path README.md) -or !(Test-Path README.zh-CN.md)) { exit 1 }; $readmePatterns = @('./assets/hero.gif','Examples','./examples/launch-examples.json','assets/launch/demo-overview.png','assets/social-preview.png'); foreach ($pattern in $readmePatterns) { Select-String -Path README.md -Pattern [regex]::Escape($pattern) | Out-Null }; $zhPatterns = @('README.md','launch-examples.json'); foreach ($pattern in $zhPatterns) { Select-String -Path README.zh-CN.md -Pattern [regex]::Escape($pattern) | Out-Null }</verify>
  <acceptance_criteria>
    - `README.md` still references `./assets/hero.gif`
    - `README.md` contains `Examples`
    - `README.md` links to `./examples/launch-examples.json`
    - `README.md` references `assets/launch/demo-overview.png` and `assets/social-preview.png`
    - `README.zh-CN.md` links back to `README.md` and the same examples corpus
  </acceptance_criteria>
  <done>The public repo entry surfaces the exact launch assets and examples visitors need first.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten contribution and issue-entry routing around real parser feedback</name>
  <files>CONTRIBUTING.md, .github/ISSUE_TEMPLATE/parser_example.md, .github/ISSUE_TEMPLATE/config.yml</files>
  <read_first>CONTRIBUTING.md, .github/ISSUE_TEMPLATE/parser_example.md, .github/ISSUE_TEMPLATE/config.yml, examples/launch-examples.json, .planning/phases/05-launch-assets-and-feedback-intake/05-CONTEXT.md</read_first>
  <action>Update `CONTRIBUTING.md` so it explicitly points contributors to three paths: `bug`, `parser examples wanted`, and `good first issue`. In `.github/ISSUE_TEMPLATE/parser_example.md`, add one prompt that asks whether the submitted sentence should also be added to `examples/launch-examples.json`. Update `.github/ISSUE_TEMPLATE/config.yml` so the issue landing surface includes a visible link to open issues filtered by the `good first issue` label and keeps the parser-example route obvious. Do not add a new feedback system or discussion board link.</action>
  <verify>if (!(Test-Path CONTRIBUTING.md) -or !(Test-Path .github/ISSUE_TEMPLATE/parser_example.md) -or !(Test-Path .github/ISSUE_TEMPLATE/config.yml)) { exit 1 }; $contribPatterns = @('bug','parser examples wanted','good first issue'); foreach ($pattern in $contribPatterns) { Select-String -Path CONTRIBUTING.md -Pattern [regex]::Escape($pattern) | Out-Null }; $parserTemplatePattern = 'examples/launch-examples.json'; Select-String -Path .github/ISSUE_TEMPLATE/parser_example.md -Pattern [regex]::Escape($parserTemplatePattern) | Out-Null; Select-String -Path .github/ISSUE_TEMPLATE/config.yml -Pattern 'good first issue' | Out-Null</verify>
  <acceptance_criteria>
    - `CONTRIBUTING.md` explicitly mentions `bug`, `parser examples wanted`, and `good first issue`
    - `.github/ISSUE_TEMPLATE/parser_example.md` mentions `examples/launch-examples.json`
    - `.github/ISSUE_TEMPLATE/config.yml` contains a visible `good first issue` route
    - No new non-GitHub feedback system is introduced
  </acceptance_criteria>
  <done>The repo makes its real-world feedback loop obvious and keeps it inside GitHub-native flows.</done>
</task>

<task type="auto">
  <name>Task 3: Add one-command validation for the full launch package</name>
  <files>scripts/validation/validate-phase-05.ps1</files>
  <read_first>.planning/phases/05-launch-assets-and-feedback-intake/05-VALIDATION.md, scripts/validation/validate-phase-04.ps1, README.md, CONTRIBUTING.md, .github/ISSUE_TEMPLATE/config.yml</read_first>
  <action>Create `scripts/validation/validate-phase-05.ps1` with the same PowerShell structure as prior phase validators. The script must first assert that these files exist: `assets/hero.gif`, `assets/social-preview.png`, `assets/launch/demo-overview.png`, `assets/launch/correction-loop.png`, `assets/launch/json-ir-view.png`, `assets/launch/asset-manifest.json`, and `examples/launch-examples.json`. Then it must assert that `README.md` contains `./assets/hero.gif` and `./examples/launch-examples.json`, `CONTRIBUTING.md` contains `good first issue`, and `.github/ISSUE_TEMPLATE/config.yml` contains `good first issue`. After those file and string checks pass, run `corepack pnpm check`, `corepack pnpm test`, and `corepack pnpm build`. Print exactly `Phase 05 validation passed.` as the final success line only after every check succeeds.</action>
  <verify>if (!(Test-Path scripts/validation/validate-phase-05.ps1)) { exit 1 }; $patterns = @('assets/hero.gif','assets/social-preview.png','assets/launch/demo-overview.png','assets/launch/correction-loop.png','assets/launch/json-ir-view.png','examples/launch-examples.json','corepack pnpm check','corepack pnpm test','corepack pnpm build','Phase 05 validation passed.'); foreach ($pattern in $patterns) { Select-String -Path scripts/validation/validate-phase-05.ps1 -Pattern [regex]::Escape($pattern) | Out-Null }</verify>
  <acceptance_criteria>
    - `scripts/validation/validate-phase-05.ps1` exists
    - The script checks all required launch asset files and the canonical examples file
    - The script checks `README.md`, `CONTRIBUTING.md`, and `.github/ISSUE_TEMPLATE/config.yml` for the required strings
    - The script runs `corepack pnpm check`, `corepack pnpm test`, and `corepack pnpm build`
    - The script prints `Phase 05 validation passed.` only on success
  </acceptance_criteria>
  <done>The launch package has one repeatable validation command before UAT and release.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] README surfaces link to the exact launch assets and examples created in Plan 01
- [ ] Contribution surfaces expose bug, parser-example, and good-first-issue routes clearly
- [ ] `scripts/validation/validate-phase-05.ps1` checks both launch-package integrity and repo health
- [ ] Public copy stays inside the already locked white-box parser positioning
</verification>

<success_criteria>

- All tasks completed
- Visitors can find launch assets and examples from the repo entry without guesswork
- Contributors can find bug, parser-example, and good-first-issue paths through GitHub-native flows
- Phase 5 gains one obvious validation command before UAT
- The release package stays honest to the current MVP instead of over-claiming scope
</success_criteria>

<output>
After completion, create `.planning/phases/05-launch-assets-and-feedback-intake/05-02-SUMMARY.md`
</output>
