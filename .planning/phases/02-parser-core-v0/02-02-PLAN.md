---
phase: 02-parser-core-v0
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - package.json
  - fixtures/parser-core-v0.ts
  - tests/parser-core-v0.test.ts
  - scripts/parse-fixtures.ts
  - scripts/validation/validate-phase-02.ps1
autonomous: true
requirements: [PARS-03, PARS-04]
user_setup: []
must_haves:
  truths:
    - "The README sentence `Initialize the model before training.` is part of the curated fixture set."
    - "The curated fixture set covers imperative, sequence, dependency, condition, and purpose patterns that match the locked Phase 2 scope."
    - "Parser output stability is checked by code, not by manual inspection only."
  artifacts:
    - path: "fixtures/parser-core-v0.ts"
      provides: "Curated parser examples with expected outputs"
      contains: "Initialize the model before training."
    - path: "tests/parser-core-v0.test.ts"
      provides: "Automated parser contract verification"
      contains: "parseSentence"
    - path: "scripts/parse-fixtures.ts"
      provides: "Fixture smoke runner"
      contains: "parserCoreFixtures"
    - path: "scripts/validation/validate-phase-02.ps1"
      provides: "Local phase validation entrypoint"
      contains: "pnpm test"
  key_links:
    - from: "tests/parser-core-v0.test.ts"
      to: "fixtures/parser-core-v0.ts"
      via: "fixture-driven parser assertions"
      pattern: "from '../fixtures/parser-core-v0'"
---

<objective>
Prove the parser core against a fixed technical-English corpus and add a minimal local validation path.

Purpose: Turn the parser from a contract stub into something we can trust against a small real fixture set before Phase 3 consumes it.
Output: Fixture-driven expectations, automated tests, a smoke runner, and a Phase 2 validation script.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-parser-core-v0/02-CONTEXT.md
@README.md
@src/parser/types.ts
@src/parser/lexicon.ts
@src/parser/parser.ts
@AGENTS.md
</context>

<threat_model>
## Trust Boundaries
- boundary: curated fixture corpus -> parser contract tests
  data: expected spans, expected summary fields, phase scope coverage
- boundary: local validation commands -> maintainers and contributors
  data: test exit codes, smoke-runner output, fixture regressions

## Threat Register
- threat_id: T-02-04
  category: Repudiation
  component: parser regression detection
  disposition: mitigate
  mitigation_plan: Store the expected outputs beside each fixture sentence and compare parser results against them in automated tests instead of relying on screenshots or memory.
- threat_id: T-02-05
  category: Tampering
  component: phase scope coverage
  disposition: mitigate
  mitigation_plan: Keep the fixture list explicit and include at least one sentence each for sequence, dependency, condition, and purpose so future changes cannot silently narrow the Phase 2 scope.
- threat_id: T-02-06
  category: Availability
  component: local verification path
  disposition: mitigate
  mitigation_plan: Provide one PowerShell validation entrypoint that runs `pnpm check`, `pnpm test`, and the fixture smoke runner so the phase can be re-verified with one command on the current Windows-first setup.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add a fixture corpus with exact expected parser outputs</name>
  <files>package.json, fixtures/parser-core-v0.ts</files>
  <read_first>.planning/phases/02-parser-core-v0/02-CONTEXT.md, README.md, src/parser/types.ts, src/parser/parser.ts</read_first>
  <action>Update `package.json` to add the scripts `test: tsx --test tests/parser-core-v0.test.ts` and `fixtures:parse: tsx scripts/parse-fixtures.ts`. Create `fixtures/parser-core-v0.ts` and export `parserCoreFixtures` as an array of fixture objects with the exact keys `id`, `input`, `expectedSpans`, and `expectedSummary`. Include these exact sentence strings: `Initialize the model before training.`, `Install the package before running the server.`, `Load the config after creating the file.`, `Run the server with the config.`, `Use the model for training.`, `Load the config to run the server.`, `If the server uses the config, load the file.`, and `When the request uses the model, create the response.`. For every fixture, store the expected normalized role list and the expected summary object so tests can assert exact parser output instead of loose heuristics.</action>
  <verify>if (!(Test-Path package.json) -or !(Test-Path fixtures/parser-core-v0.ts)) { exit 1 }; $packagePatterns = @('"test": "tsx --test tests/parser-core-v0.test.ts"','"fixtures:parse": "tsx scripts/parse-fixtures.ts"'); foreach ($pattern in $packagePatterns) { Select-String -Path package.json -Pattern [regex]::Escape($pattern) | Out-Null }; $fixturePatterns = @('parserCoreFixtures','Initialize the model before training.','Install the package before running the server.','Load the config after creating the file.','Run the server with the config.','Use the model for training.','Load the config to run the server.','If the server uses the config, load the file.','When the request uses the model, create the response.','expectedSpans','expectedSummary'); foreach ($pattern in $fixturePatterns) { Select-String -Path fixtures/parser-core-v0.ts -Pattern [regex]::Escape($pattern) | Out-Null }</verify>
  <acceptance_criteria>
    - `package.json` contains `"test": "tsx --test tests/parser-core-v0.test.ts"`
    - `package.json` contains `"fixtures:parse": "tsx scripts/parse-fixtures.ts"`
    - `fixtures/parser-core-v0.ts` exports `parserCoreFixtures`
    - `fixtures/parser-core-v0.ts` contains all eight locked fixture sentences
    - `fixtures/parser-core-v0.ts` contains `expectedSpans` and `expectedSummary` for every fixture
  </acceptance_criteria>
  <done>The phase gets a fixed parser corpus that captures exactly what Phase 2 promises to support.</done>
</task>

<task type="auto">
  <name>Task 2: Add automated tests and a fixture smoke runner</name>
  <files>tests/parser-core-v0.test.ts, scripts/parse-fixtures.ts</files>
  <read_first>fixtures/parser-core-v0.ts, src/index.ts, src/parser/parser.ts</read_first>
  <action>Create `tests/parser-core-v0.test.ts` using Node's built-in test API with `tsx --test`. Import `parseSentence` from `../src/index` and `parserCoreFixtures` from `../fixtures/parser-core-v0`. For each fixture, assert that the parser returns the exact `expectedSpans` sequence and the exact `expectedSummary` object. Add one explicit regression assertion for the README example so `Initialize`, `model`, `before`, and `training` stay mapped to `action`, `object`, `relation`, and `action` respectively. Create `scripts/parse-fixtures.ts` to loop through `parserCoreFixtures`, run `parseSentence`, compare the result to the expected output, print the fixture id and input for each pass, print the mismatched actual and expected JSON for each failure, and exit with code `1` if any fixture fails.</action>
  <verify>if (!(Test-Path tests/parser-core-v0.test.ts) -or !(Test-Path scripts/parse-fixtures.ts)) { exit 1 }; $testPatterns = @('parseSentence','parserCoreFixtures','Initialize the model before training.','action','object','relation'); foreach ($pattern in $testPatterns) { Select-String -Path tests/parser-core-v0.test.ts -Pattern [regex]::Escape($pattern) | Out-Null }; $scriptPatterns = @('parserCoreFixtures','parseSentence','process.exitCode = 1','JSON.stringify'); foreach ($pattern in $scriptPatterns) { Select-String -Path scripts/parse-fixtures.ts -Pattern [regex]::Escape($pattern) | Out-Null }</verify>
  <acceptance_criteria>
    - `tests/parser-core-v0.test.ts` imports `parseSentence`
    - `tests/parser-core-v0.test.ts` imports `parserCoreFixtures`
    - `tests/parser-core-v0.test.ts` contains an explicit regression assertion for `Initialize the model before training.`
    - `scripts/parse-fixtures.ts` imports `parserCoreFixtures` and `parseSentence`
    - `scripts/parse-fixtures.ts` sets `process.exitCode = 1` when a fixture fails
    - `scripts/parse-fixtures.ts` prints JSON for mismatched actual and expected output
  </acceptance_criteria>
  <done>Fixture coverage becomes executable and regressions can be caught without opening the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Add a one-command local validation entrypoint for Phase 2</name>
  <files>scripts/validation/validate-phase-02.ps1</files>
  <read_first>package.json, tests/parser-core-v0.test.ts, scripts/parse-fixtures.ts, scripts/validation/validate-phase-01.ps1</read_first>
  <action>Create `scripts/validation/validate-phase-02.ps1` as a simple PowerShell script that runs exactly these commands from repo root in order: `pnpm install`, `pnpm check`, `pnpm test`, and `pnpm fixtures:parse`. Make the script stop on the first failure, print which command is running, and end with one success line only after all commands pass. Keep the script focused on local parser validation only; do not add extra formatting, screenshots, or unrelated checks.</action>
  <verify>if (!(Test-Path scripts/validation/validate-phase-02.ps1)) { exit 1 }; $patterns = @('pnpm install','pnpm check','pnpm test','pnpm fixtures:parse'); foreach ($pattern in $patterns) { Select-String -Path scripts/validation/validate-phase-02.ps1 -Pattern [regex]::Escape($pattern) | Out-Null }</verify>
  <acceptance_criteria>
    - `scripts/validation/validate-phase-02.ps1` contains `pnpm install`
    - `scripts/validation/validate-phase-02.ps1` contains `pnpm check`
    - `scripts/validation/validate-phase-02.ps1` contains `pnpm test`
    - `scripts/validation/validate-phase-02.ps1` contains `pnpm fixtures:parse`
  </acceptance_criteria>
  <done>Phase 2 can be revalidated locally with one obvious command on the current Windows setup.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `fixtures/parser-core-v0.ts` contains the locked eight-sentence corpus and expected outputs
- [ ] `tests/parser-core-v0.test.ts` asserts the exact parser contract for every fixture
- [ ] `scripts/parse-fixtures.ts` fails loudly on mismatches
- [ ] `scripts/validation/validate-phase-02.ps1` runs install, typecheck, test, and fixture smoke checks in one path
</verification>

<success_criteria>

- All tasks completed
- The README example is permanently captured as a regression fixture
- The phase has deterministic tests for sequence, dependency, condition, and purpose sentences
- Parser output stability is checked by code before the demo phase begins
- Contributors have one obvious local command path to validate the parser core
</success_criteria>

<output>
After completion, create `.planning/phases/02-parser-core-v0/02-02-SUMMARY.md`
</output>
